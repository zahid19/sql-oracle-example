(SELECT LISTAGG (DESCRIPTION, '; ') WITHIN GROUP (ORDER BY DESCRIPTION) AS DESCRIPTION
                FROM CATEGORIES CA, ITEM_CATEGORY ITCA
                WHERE CA.CAT_ID = ITCA.CAT_ID AND ITCA.ITEM_ID = IT.ITEM_ID)
                CAT_NAME,